# Admin sign-in for /admin/login
ADMIN_EMAIL=admin@tculverrealty.com
ADMIN_PASSWORD=culver2026
ADMIN_NAME=Site Admin
JWT_SECRET=replace-with-a-long-random-string

# Frontend origin(s) allowed to call the API (comma separated)
CORS_ORIGINS=http://localhost:3000

# Optional: use MongoDB instead of the on-disk JSON store
# MONGO_URL=mongodb://localhost:27017
# DB_NAME=culver_realty

# Optional: real Zillow ingest provider (leave blank -> demo extractor, matches live site)
# RAPIDAPI_KEY=
# APIFY_TOKEN=
